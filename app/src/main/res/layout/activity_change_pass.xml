<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_change_pass"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.diarydemo.ChangePassActivity"
    android:background="@drawable/bg_login">

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true">

        <TextView
            android:id="@+id/activity_change_pass_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="请输入日记本的旧密码"
            android:textSize="20dp"
            android:textColor="@color/loginTextColor" />

        <LinearLayout
            android:id="@+id/activity_change_pass_inputline"
            android:layout_below="@id/activity_change_pass_title"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="30dp"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:orientation="horizontal"
            android:padding="0dp">

            <EditText
                android:id="@+id/activity_change_pass_password"
                android:layout_width="100dp"
                android:layout_height="match_parent"
                android:layout_centerHorizontal="true"
                android:background="#FFFFFF"
                android:maxLines="1"
                android:textSize="20dp"
                android:inputType="textPassword"/>

            <Button
                android:id="@+id/activity_change_pass_btnok"
                android:layout_width="70dp"
                android:layout_height="match_parent"
                android:padding="0dp"
                android:text="确定"
                android:background="@color/loginTextColor"/>

        </LinearLayout>

    </RelativeLayout>

</RelativeLayout>
